{% extends 'front.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
 <center>
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_f</th>
                <td>{{ facture.id_f }}</td>
            </tr>
            <tr>
                <th>Id_c</th>
                <td>{{ facture.reservation }}</td>
            </tr>
        </tbody>
    </table>

    {#<a href="{{ path('app_facture_index') }}">back to list</a>#}

    <a href="{{ path('app_facture_edit', {'id_f': facture.id_f}) }}" class="btn btn-primary submit-btn">edit</a>

    {{ include('facture/_delete_form.html.twig') }}
     </center>
{% endblock %}
