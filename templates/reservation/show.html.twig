{% extends 'front.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
<center>  
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero reservation</th>
                <td>{{ reservation.id_res }}</td>
            </tr>
            <tr>
                <th>Date debut de reservation</th>
                <td>{{ reservation.dateDebut ? reservation.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date fin de reservation</th>
                <td>{{ reservation.dateFin ? reservation.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    {#<a href="{{ path('app_reservation_index') }}">back to list</a>#}

    <a href="{{ path('app_reservation_edit', {'id_res': reservation.id_res} )  }}" class="btn btn-primary submit-btn">editer</a>

    {{ include('reservation/_delete_form.html.twig') }}
    </center>
{% endblock %}
